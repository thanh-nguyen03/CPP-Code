#include <iostream>
#include <math.h>
#include <algorithm>
#include <string>
using namespace std;

typedef long long ll;
#define fastread() (ios_base:: sync_with_stdio(false),cin.tie(NULL));

int n, m, a, b;

void solve() {
    cin >> n >> m >> a >> b;
    cout << 1 << " " << 1 << " " << n << " " << m << endl;
}

int main() {
    fastread();
    int t;
    cin >> t;
    while (t--)
        solve();
}